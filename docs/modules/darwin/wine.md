# darwin → wine

**源文件**: `modules/darwin/wine.nix`  
**选项前缀**: `config.modules.macos`

## 选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enable` | bool | Whether to use wine"; # macos 上安装 wine 模拟工具 | 是否启用 |
| `crossover.enable` | bool | false; # 使用 crossosver 工具 |  |

