# darwin → duti

**源文件**: `modules/darwin/duti.nix`  
**选项前缀**: `config.wrapper`

## 选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enable` | bool | macos set file default open app | 是否启用 |
| `enable` | bool | false |  |

## 条件分支

- 当 `cfg.enable` 为真时激活

