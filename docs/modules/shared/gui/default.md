# shared → gui → default

**源文件**: `modules/shared/gui/default.nix`  
**选项前缀**: `config.modules.shared.gui.default`

## 选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enable` | bool | whether to use gui apps | 是否启用 |

