# shared → gui → terminal → alacritty

**源文件**: `modules/shared/gui/terminal/alacritty.nix`  
**选项前缀**: `config.modules.gui.terminal`

## 选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enable` | bool | Whether to use alacritty | 是否启用 |

## 平台差异

本模块包含 Linux 专属配置。

