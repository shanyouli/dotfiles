# shared → dev → nix

**源文件**: `modules/shared/dev/nix.nix`  
**选项前缀**: `config.modules.shared.dev.nix`

## 选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enable` | bool | Whether to Nix Language | 是否启用 |
| `lspPkg` | package | nil | `nil` |

