# shared → dev → zig

**源文件**: `modules/shared/dev/zig.nix`  
**选项前缀**: `config.modules.dev`

## 选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enable` | bool | Whether to zig language | 是否启用 |
| `package` | null or package | zig | `zig`；Whether to zig package |

