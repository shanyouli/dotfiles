# shared → tui → rsync

**源文件**: `modules/shared/tui/rsync.nix`  
**选项前缀**: `config.modules.rsync`

## 选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enable` | bool | Whether to use rsync | 是否启用 |
| `package` | types.package | pkgs.rsync |  |

