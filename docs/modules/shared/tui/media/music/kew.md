# shared → tui → media → music → kew

**源文件**: `modules/shared/tui/media/music/kew.nix`  
**选项前缀**: `config.modules.media.music`

## 选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enable` | bool | Whether to using kew. | 是否启用 |
| `package` | package | kew | `kew` |

