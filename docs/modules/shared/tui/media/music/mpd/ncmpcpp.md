# shared → tui → media → music → mpd → ncmpcpp

**源文件**: `modules/shared/tui/media/music/mpd/ncmpcpp.nix`  
**选项前缀**: `config.modules.media.music.mpd`

## 选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enable` | bool | Whether to use ncmpcpp | 是否启用 |
| `config` | types.lines | "" | Extra directives added to the end of ncmpcpp's configuration file |

