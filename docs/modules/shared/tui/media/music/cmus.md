# shared → tui → media → music → cmus

**源文件**: `modules/shared/tui/media/music/cmus.nix`  
**选项前缀**: `config.modules.media.music`

## 选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enable` | bool | Whether to use cmus | 是否启用 |

