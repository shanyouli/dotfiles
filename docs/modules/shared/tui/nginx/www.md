# shared → tui → nginx → www

**源文件**: `modules/shared/tui/nginx/www.nix`  
**选项前缀**: `config.modules.nginx`

## 选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enable` | bool | Whether to use startpage | 是否启用 |

