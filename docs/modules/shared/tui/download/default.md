# shared → tui → download → default

**源文件**: `modules/shared/tui/download/default.nix`  
**选项前缀**: `config.modules.shared.tui.download.default`

## 选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enable` | bool | Whether to using download manager | 是否启用 |

