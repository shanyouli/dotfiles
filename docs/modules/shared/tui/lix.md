# shared → tui → lix

**源文件**: `modules/shared/tui/lix.nix`  
**选项前缀**: `config.modules.tui`

## 选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enable` | bool | Whether to use lix | 是否启用 |
| `package` | package | lix | `lix` |

