# shared → shell → plugins → nix-index

**源文件**: `modules/shared/shell/plugins/nix-index.nix`  
**选项前缀**: `config.modules.shared.shell.plugins.nix-index`

## 选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enable` | bool | Whether to nix-index | 是否启用 |

