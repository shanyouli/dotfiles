# shared → shell → plugins → nix-your-shell

**源文件**: `modules/shared/shell/plugins/nix-your-shell.nix`  
**选项前缀**: `config.modules.shell`

## 选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enable` | bool | Whether to use nix-your-shell | 是否启用 |

